<h1 class="current-category"><%= params[:genre] %></h1>
	<% if @movies.count ==0 %>
		<h2>There are currently no movies in this genre</h2>
	<% else %>
		<% @movies.each do |movie| %>
		<h2> <%= link_to movie.title, movie_path(movie)%></h2>
	<% end %>
<% end %>

